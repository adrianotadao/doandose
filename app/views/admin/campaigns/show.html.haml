%h2= @campaign.title
%dl
  %dt
    %dd= @campaign.active
    %dd= @campaign.blood
    %dd= @campaign.company
    %dd= @campaign.content
    %dd= @campaign.quantity
    %dd= @campaign.expired_at

= link_to t('form.buttons.back'), admin_campaigns_path, :class => 'button back'
= link_to t('form.buttons.delete'), [:admin, @campaign], :confirm => t('flash.blood.delete.advertise'), :method => :delete, :class => "button delete"
= link_to t('form.buttons.edit'), edit_admin_campaign_path(@campaign), :class => "edit"