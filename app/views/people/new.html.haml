%h1= t('label.user.user_step.title')
.line
#people
  .new= render :partial => '/people/minimal_form'
  #map

- content_for :javascript do
  :javascript
    new SearchMap(#{coordenations(@addresses.to_a)})
