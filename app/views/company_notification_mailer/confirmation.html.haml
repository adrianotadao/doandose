%h2= t('mailer.salute')
%p= t('mailer.confirmation_participation_title') #{@notification.title}

.counter
  %b.count
    = @notification.person_notifications.non_canceleds.count
  #{t('label.participant')} -
  #{t('label.remaining')}
  %b.remaining #{@notification.remaining}