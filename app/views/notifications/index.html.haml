#notifications
  %h1 Notificacoes
  -unless @notifications.blank?
    - for notification in @notifications
      .content
        %div
          %h2= notification.title
          .description= truncate(notification.observation, length:380)

        #mini_map

        .action
          = link_to t('buttons.participe'), confirm_notification_path(notification), class: 'button red'
          = link_to t('buttons.show'), notification, class: 'button gray'

  - else
    .empty_list= t('label.empty')