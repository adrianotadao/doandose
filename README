== Gmap

* Creating markers

  --Person--
  Marker.person(COORDINATE, OPTIONS)

  --Company--
  Marker.company(COORDINATE, OPTIONS)

  --User position--
  Marker.userPosition(COORDINATE, OPTIONS)

== Redis
redis-server

== Resque
QUEUE=* rake resque:work

== Populate
rake development:populate:run

== Mongo
mongo doandose_development
db.addresses.ensureIndex( { loc : "2d" } )

== Test
bundle exec guard start --no-interactions


ssh voraz.myhosting
kill -9 $(pidof nginx)
nginx

EARTH_RADIUS = 6371 KM