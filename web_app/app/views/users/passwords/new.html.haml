-if @errors && @errors.size > 0
  .validation
    %h2 Os seguintes erros foram encontrados:

    - for error in @errors
      %ul
        %li= error
        
= form_tag '/usuario/esqueci-minha-senha/', :class => 'forgot_password' do
  %dl.form
    %dt Login ou Email:
    %dd= text_field_tag :email

  = submit_tag 'enviar reset', :class => 'submit'
